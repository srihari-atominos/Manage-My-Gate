import { EventEmitter } from 'events';

// Core native event emitter for the onboarding wizard feature domain
export const onboardingWizardEvents = new EventEmitter();

export default onboardingWizardEvents;
